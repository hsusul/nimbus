{
  "name": "@nimbus/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@nimbus/config": "workspace:*",
    "@nimbus/contracts": "workspace:*",
    "@nimbus/db": "workspace:*",
    "@nimbus/logger": "workspace:*",
    "@nimbus/storage": "workspace:*",
    "bullmq": "^5.56.0",
    "ioredis": "^5.6.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
